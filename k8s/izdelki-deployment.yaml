apiVersion: apps/v1
kind: Deployment
metadata:
  name: primerjalnikIzdelkov-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: primerjalnikIzdelkov
  template:
    metadata:
      labels:
        app: primerjalnikIzdelkov
    spec:
      containers:
        - image: rsoskupina50/izdelki:2022-12-10-17-37-27
          name: primerjalnikIzdelkov
          env:
            - name: KUMULUZEE_DATASOURCES0_CONNECTIONURL
              value: jdbc:postgresql://mouse.db.elephantsql.com/zzgixwih
            - name: KUMULUZEE_DATASOURCES0_USERNAME
              value: zzgixwih
            - name: KUMULUZEE_DATASOURCES0_PASSWORD
              value: XwMg-3JIpwLWNylgwAP0bcQk3_dZmVFa
          ports:
            - containerPort: 8080
              name: server
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/live
              port: server
            initialDelaySeconds: 20
            periodSeconds: 3
          readinessProbe:
              httpGet:
                path: /health/ready
                port: server
              initialDelaySeconds: 20
              periodSeconds: 3

---

apiVersion: v1
kind: Service
metadata:
  name: primerjalnikIzdelkov
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
      targetPort: server
  selector:
    app: primerjalnikIzdelkov
